import React, { Component } from "react"

class App extends Component {
    render = () => {
        return (
            <div>
                <input type="text" />
                <button>add</button>
            </div>
        )
    }
}

export default App
